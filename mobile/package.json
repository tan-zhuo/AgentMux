{
  "name": "agentmux-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Android shell for AgentMux serve mode — a WebView onto your agentmux --serve instance",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.6.0"
  }
}
