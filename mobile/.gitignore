# The android/ project is generated by `npx cap add android` — in CI on every
# build, locally whenever someone wants Android Studio. Only the config, the
# connect page and this manifest of intent are the source of truth.
android/
node_modules/
package-lock.json
