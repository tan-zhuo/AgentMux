# The android/ project is committed — it carries real source now: the core
# service, the plugin, the manifest. What stays out is generated per build:
# the embedded Go core (compiled by CI into jniLibs) and node plumbing.
android/app/src/main/jniLibs/
node_modules/
package-lock.json
